name: Format, Lint, Test, Coverage, and Type Checking

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  format_code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install coverage mypy
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      
      - id: files
        name: Set up dependency for gettting changed files
        uses: jitterbit/get-changed-files@v1
      
      - name: Get all added or modified files
        id: get_files
        run: |
          echo "FILES=$( echo ${{ steps.files.outputs.added_modified }} |
                          tr " " "\n" |
                          grep -E "\.py$" |
                          tr "\n" " ")" >> "$GITHUB_ENV"
          echo "OTHER_FILES=$( echo ${{ steps.files.outputs.added_modified }} |
                          tr " " "\n" |
                          grep -E "\.txt$|\.json$|\.csv$"  |
                          tr "\n" " ")" >> "$GITHUB_ENV"
          echo "files_output=${{ env.FILES }}" >> $GITHUB_OUTPUT
          echo "other_files_output=${{ env.OTHER_FILES }}" >> $GITHUB_OUTPUT

      - name: Format code with Black
        if: ${{ env.FILES != '' && github.ref != 'refs/heads/main' }}
        run: |
          black ${{ env.FILES }}

      - name: Commit and push formatting changes
        if: ${{ env.FILES != '' && github.ref != 'refs/heads/main' }}
        env:
          BRANCH_NAME: ${{ github.head_ref || github.ref }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m "Apply Black Formatting" --allow-empty
          CLEAN_BRANCH_NAME=${BRANCH_NAME/refs\/heads\//}
          git push -f https://${GITHUB_TOKEN}@github.com/${{ github.repository }}/HEAD:$CLEAN_BRANCH_NAME
  
  lint_test_type_check_unauthorized_changes:
    needs: format_code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install coverage mypy
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      
      - name: Use FILES and OTHER_FILES from format_code job
        run: |
          FILES="${{ needs.format_code.outputs.files_output }}"
          OTHER_FILES="${{ needs.format_code.outputs.other_files_output }}"
          echo "Using FILES: $FILES"
          echo "Using OTHER_FILES: $OTHER_FILES"

      - name: Lint with flake8
        if: ${{ env.FILES != '' }}
        run: |
          # stop the build if there are Python syntax errors or undefined names
          flake8 ${{ env.FILES }}

      - name: Test coverage
        id: coverage
        run: |
          coverage run --rcfile=.github/.coveragerc
          coverage report --rcfile=.github/.coveragerc
          coverage json --rcfile=.github/.coveragerc
          echo "coverage_percentage=$(jq -r '.totals.percent_covered_display' .github/workflows/coverage.json)" >> $GITHUB_OUTPUT
          echo "coverage_percentage=$(jq -r '.totals.percent_covered_display' .github/workflows/coverage.json)"

      - name: Run mypy on the current branch
        run: |
          python -m mypy . 2>&1 | tee mypy_current_branch_report.txt || true
          grep -c "error:" mypy_current_branch_report.txt > mypy_current_branch_error_count.txt

      - name: Set current branch error count as output
        id: set_current_branch_error_count
        run: echo "error_count=$(cat mypy_current_branch_error_count.txt)" >> $GITHUB_OUTPUT

      - name: Run mypy on the main branch
        if: github.ref != 'refs/heads/main'
        run: |
          git checkout main
          python -m mypy . 2>&1 | tee mypy_main_branch_report.txt || true
          grep -c "error:" mypy_main_branch_report.txt > mypy_main_branch_error_count.txt

      - name: Set main branch error count as output
        id: set_main_branch_error_count
        if: github.ref != 'refs/heads/main'
        run: echo "error_count=$(cat mypy_main_branch_error_count.txt)" >> $GITHUB_OUTPUT

      - name: Compare mypy reports
        if: github.ref != 'refs/heads/main'
        run: |
          CURRENT_ERRORS=${{ steps.set_current_branch_error_count.outputs.error_count }}
          MAIN_ERRORS=${{ steps.set_main_branch_error_count.outputs.error_count }}
          echo "Current branch mypy errors: $CURRENT_ERRORS"
          echo "Main branch mypy errors: $MAIN_ERRORS"
          if [ $CURRENT_ERRORS -gt $MAIN_ERRORS ]; then
            echo "Mypy error count has increased from the main branch to the current branch ($MAIN_ERRORS -> $CURRENT_ERRORS)."
            exit 1
          else
            echo "No increase in mypy errors found."
          fi
        continue-on-error: true

      - name: Checkout current branch
        if: github.ref != 'refs/heads/main'
        run: git checkout ${{ github.head_ref }}

      - name: Update Coverage and Mypy Badges
        run: |
          chmod +x ./.github/workflows/update_badge.sh
          chmod +x ./.github/workflows/update_mypy_badge.sh
          ./.github/workflows/update_badge.sh
          ./.github/workflows/update_mypy_badge.sh $(cat mypy_current_branch_error_count.txt)

      - name: Commit & Push Badge Updates
        uses: EndBug/add-and-commit@v7
        with:
          message: "Update coverage and mypy badges on README"
          add: "README.md .github/badges/"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.head_ref }}

      - name: Comment on Coverage and Mypy Errors
        env:
          URL: ${{ github.event.pull_request.comments_url }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          MY_ERROR_COUNT=$(cat mypy_current_branch_error_count.txt)
          MAIN_ERRORS=${{ steps.set_main_branch_error_count.outputs.error_count }}
          if [ -n "$URL" ]; then
            PYTEST_COVERAGE="Current Coverage: ${{ steps.coverage.outputs.coverage_percentage }}%"
            echo "$PYTEST_COVERAGE"
            MYPY_ERRORS="Mypy errors on ${{ github.head_ref }} branch: $MY_ERROR_COUNT"
            MYPY_ERRORS="$MYPY_ERRORS\nMypy errors on main branch: $MAIN_ERRORS"
            DIFF=$((MY_ERROR_COUNT - MAIN_ERRORS))
            if [ $DIFF -gt 0 ]; then
              MYPY_ERRORS="$MYPY_ERRORS\nDifference: $DIFF more errors on ${{ github.head_ref }} branch"
            elif [ $DIFF -lt 0 ]; then
              DIFF=$((DIFF * -1))
              MYPY_ERRORS="$MYPY_ERRORS\nDifference: $DIFF fewer errors on ${{ github.head_ref }} branch"
            else
              MYPY_ERRORS="$MYPY_ERRORS\nDifference: No difference in error counts"
            fi
            echo "$MYPY_ERRORS"
            COMMIT_MESSAGE="$PYTEST_COVERAGE\n\n$MYPY_ERRORS"
            echo "$COMMIT_MESSAGE"
            curl \
              -X POST \
              $URL \
              -H "Content-Type: application/json" \
              -H "Authorization: token $GITHUB_TOKEN" \
              --data "{\"body\": \"$COMMIT_MESSAGE\"}"
          fi
          
      - name: Check for Unauthorized Changes
        run: |
          paths_to_protect=(
            'RUFAS/scenario_manager.py'
          )
          unauthorized_changes=false
          
          for path in "${paths_to_protect[@]}"; do
            if (echo ${{ env.FILES }}; echo ${{ env.OTHER_FILES }}) | grep -q "$path"; then
              unauthorized_changes=true
              unauthorized_paths+="$path, "
            fi
          done
  
          if $unauthorized_changes; then
            echo "Unauthorized changes detected in protected files: ${unauthorized_paths%, }"
            echo "Review and remove these changes if they are not intended."
            exit 1
          else
            echo "No unauthorized changes detected."
          fi
